import React from 'react';
import './Header.css';

const Header = () => {
    return (
        <header className='header-title'>
            <h1>Image Search Engine</h1>
        </header>
    );
};

export default Header;
